# ════════════════════════════════════════════════════════════════
# requirements.txt - Autonomous Agent Dependencies
# ════════════════════════════════════════════════════════════════
# Install dengan: pip install -r requirements.txt

# ────────────────────────────────────────
# Core LLM & Inference
# ────────────────────────────────────────

# HTTP client untuk Ollama API (async support)
httpx>=0.27.0

# HuggingFace Transformers (fallback dari Ollama)
transformers>=4.40.0

# PyTorch (dibutuhkan oleh transformers)
# Untuk GPU: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.2.0

# Accelerate untuk efficient model loading
accelerate>=0.28.0

# ────────────────────────────────────────
# Memory & Vector Storage
# ────────────────────────────────────────

# ChromaDB - persistent vector database untuk episodic memory
chromadb>=0.4.24

# FAISS - fast similarity search untuk semantic memory
# CPU only (lebih ringan):
faiss-cpu>=1.8.0
# GPU version (jika ada GPU):
# faiss-gpu>=1.7.4

# Sentence Transformers - embedding model untuk RAG
sentence-transformers>=2.7.0

# ────────────────────────────────────────
# Tools
# ────────────────────────────────────────

# DuckDuckGo Search - web search tanpa API key
duckduckgo-search>=5.3.0

# ────────────────────────────────────────
# Utilities
# ────────────────────────────────────────

# Async support
asyncio>=3.4.3

# Data processing
numpy>=1.26.0

# JSON & YAML
pyyaml>=6.0.1

# Logging yang lebih baik (opsional)
rich>=13.7.0

# ────────────────────────────────────────
# Testing (opsional, untuk development)
# ────────────────────────────────────────

# pytest>=7.4.0
# pytest-asyncio>=0.23.0
